version: 2

build:
  os: ubuntu-22.04
  tools:
    python: miniconda-latest
  jobs:
    pre_build:
      - python docs/rtd.pre.py
    post_build:
      - python docs/rtd.post.py

conda:
  environment: envs/docenv-linux-cpu.yaml

sphinx:
  builder: html
  configuration: docs/conf.py